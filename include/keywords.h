typedef enum {
    // python keywords
    _False, _await, _else, _import, _pass,
    _None, _break, _except, _in, _raise,
    _True, _class, _finally, _is, _return,
    _and, _continue, _for, _lambda, _try,
    _as, _def, _from, _nonlocal, _while,
    _assert, _del, _global, _not, _with,
    _async, _elif, _if, _or, _yield
} PythonKeyword;
